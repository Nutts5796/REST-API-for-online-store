![Скриншот](images/Go.png)
# 🛒 Mini REST-API-for-online-store

Это REST API для простого интернет-магазина, разработанного на Go + Fiber с PostgreSQL в качестве базы данных. Основная цель — обеспечить функционал для пользователей и товаров, включая регистрацию, аутентификацию, а также получение данных о товарах и пользователях.

# 📌 Стек технологий

Go + Fiber — быстрый и удобный веб-фреймворк.
PostgreSQL — надёжная реляционная база данных.
Docker + Compose — для лёгкого деплоя и поднятия сервисов.
JWT Auth — безопасная аутентификация пользователей.
Makefile — упрощает команды для запуска и разработки.

# 🚀 Быстрый старт

✅ Предпосылки
Убедись, что установлены:

Docker
Docker Compose
Go

# 🔐 Аутентификация и управление пользователями

Приложение реализует аутентификацию пользователей через JWT токены. Есть базовые механизмы работы с пользователями:

✅ Регистрация пользователя
Ожидает на вход JSON с полями:
    {
  "username": "имя_пользователя",
  "email": "email@domain.com",
  "password": "пароль"
}

Хэширует пароль.
Сохраняет пользователя в базу данных PostgreSQL (users).
Возвращает статус 201 Created и сообщение об успешной регистрации.

✅ Вход пользователя /login
Ожидает JSON с полями:
    {
  "email": "email@domain.com",
  "password": "пароль"
}
Проверяет, существует ли пользователь с указанным email.
Сравнивает хэш пароля с введённым паролем.
При успешной проверке генерирует JWT токен (сейчас возвращается dummy-token, в будущем легко заменить на полноценный JWT).
Возвращает токен в JSON:
    {
  "token": "jwt-token"
}

✅ Logout /logout и Refresh Token /refresh-token
Заглушки: возвращают статус 200 OK и сообщение.
Можно расширить: например, добавить список отозванных токенов или обновление refresh-токена.

# 📦 Работа с товарами (Products)

Позволяет получать список товаров из базы данных PostgreSQL:

✅ Получить список товаров /products
Возвращает массив товаров в формате JSON:
    [
  {
    "id": 1,
    "name": "Название товара",
    "description": "Описание",
    "price": 99.99
  },
  ...
]

Данные берутся из таблицы products.
Доступ открыт для всех (аутентификация не требуется).

# 👥 Работа с пользователями (Users)

Позволяет получить список всех зарегистрированных пользователей. Это закрытый эндпоинт, доступный только аутентифицированным пользователям.

✅ Получить список пользователей /users
Возвращает JSON с массивом пользователей:
    {
  "users": [
    {
      "id": 1,
      "username": "user1",
      "email": "user1@example.com"
    },
    ...
  ]
}

Получает данные из таблицы users.
В будущем можно добавить роли (например, user, admin).

# 🏗 Как устроено приложение внутри

Приложение имеет чистую структуру кода, удобно расширяемую:

├── Dockerfile                # Описание сборки образа для Go-приложения
├── Makefile                  # Набор команд для удобной работы (run, down, migrate, lint, test)
├── READMEeng.md              # Документация на английском языке
├── READMErus.md              # Документация на русском языке
├── cmd
│   └── main.go               # Точка входа в приложение: инициализация сервера, подключение к БД
├── docker-compose.yml        # Конфигурация Docker Compose (Go API + PostgreSQL)
├── go.mod                    # Зависимости Go-модуля
├── go.sum                    # Контрольные суммы модулей
├── internal
│   ├── handler               # Пакет с обработчиками HTTP-запросов
│   │   ├── auth.go           # Регистрация, логин, логаут, обновление токена
│   │   ├── product.go        # Получение списка товаров
│   │   └── routes.go         # Регистрация маршрутов приложения
│   └── middleware
│       └── jwt.go            # Middleware для проверки JWT токенов (аутентификация)
├── migrations
│   └── init.sql              # Начальная миграция для создания таблиц users и products
└── model
    └── user.go               # Структуры моделей, описание пользователя (User)


# ⚡ Как это работает шаг за шагом

При запуске docker-compose up --build:
Поднимается контейнер с базой данных PostgreSQL.
Приложение на Go запускается и подключается к БД.
Миграция (init.sql) создаёт таблицы при необходимости.
Пользователь может:
Зарегистрироваться, отправив POST /register.
Залогиниться, получив JWT token по POST /login.
Делать запросы к открытым или защищённым эндпоинтам.

# 🔧 Возможные улучшения (Roadmap)

✅ Реализовать полноценную JWT-аутентификацию с refresh токенами.
✅ Добавить роли пользователей (admin, user).
✅ Добавить эндпоинты CRUD для управления товарами:
POST /products — добавить товар.
PUT /products/:id — обновить товар.
DELETE /products/:id — удалить товар.
✅ Интеграция со Swagger для автоматической генерации API-документации.
✅ Добавить покрытие интеграционными тестами с использованием testcontainers-go.

# 🎯 Кратко: Зачем это приложение?

Это минималистичный, но продуманный бэкэнд для интернет-магазина, с простой архитектурой и базовым функционалом:

Основа для реального проекта с Go + Fiber.
Отличный пример архитектуры REST API с JWT и Docker.
Готовая база для расширения и усложнения бизнес-логики.